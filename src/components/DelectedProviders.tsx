import { FC } from 'react'

interface DelectedProvidersProps {
  
}

const DelectedProviders: FC<DelectedProvidersProps> = ({}) => {
  return <div>DelectedProviders</div>
}

export default DelectedProviders