import { FC } from 'react'

interface RejectedProvidersProps {
  
}

const RejectedProviders: FC<RejectedProvidersProps> = ({}) => {
  return <div>RejectedProviders</div>
}

export default RejectedProviders